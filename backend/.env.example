# --- Local development (backend running on host, Postgres on localhost) ---
DATABASE_URL=postgresql://realestate:CHANGE_ME@localhost:5432/realestate
POSTGRES_PASSWORD=CHANGE_ME

# --- PaaS deployment (Neon + Railway/Render) ---
# DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/realestate?sslmode=require
# CORS_ORIGINS=https://your-app.vercel.app
